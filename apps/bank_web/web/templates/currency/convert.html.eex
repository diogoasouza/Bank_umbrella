
<%= form_for @changeset, currency_path(@conn, :convert), fn f -> %>
<div class="input-group">
    <%= label f, "Currency:", class: "input-label"%>
    <%= select(f, :currency, ["Real": "BRL", "Dollar": "USD", "Euro": "EUR"]) %>
</div>
  <%= submit "Converter" %>

<% end %>
